import { defineMessages } from 'react-intl';

export const scope = 'src.app.containers.Template';

export default defineMessages({
  changeLocaleButton: {
    id: `${scope}.changeLocaleButton`,
  },
});
